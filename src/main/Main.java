package main;

import java.io.File;

public class Main {
    public static void main(String[] args) {
        for(int i = 0; i < 1; i++)
            new TSP();

        // PercentGraph graph = new PercentGraph();
        // graph.read(1, new File("src/percentGraph/rat99.txt"));
        // graph.read(2, new File("src/percentGraph/kroB200.txt"));
        // graph.read(3, new File("src/percentGraph/pr299.txt"));
        // graph.repaint();
    }
}
